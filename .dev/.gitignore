# ---------------------------------------------------------------------------
# 开发环境里**不入库**的东西
#
# 放在这个目录而不是根 .gitignore：规则只与 .dev/ 有关，就近维护；
# 根 .gitignore 只需要管仓库级的东西（模块、编辑器、密钥配置等）。
#
# 本文件里的路径都相对 .dev/ 解析。
# 需要入库的：compose.yml、odoo.conf、docker/（镜像与入口脚本）、scripts/。
# ---------------------------------------------------------------------------

# 运行时数据：Postgres 数据目录与 Odoo filestore（不在库里，随 task up 生成）
data/

# db-sync.sh 的备份 zip（含业务数据，绝不入库）
backups/

# 本地覆盖：换端口 / 换工程名 / 改 PUID 时用，避免改被跟踪的 compose.yml
.env

# `task odoo-src` 从镜像里导出的核心源码副本（只为编辑器跳转，随时可删可重建）
.cache/

# 容器内配置要入库。根 .gitignore 里的 `*.conf` 是为了挡服务器配置，
# 这里放行 —— 下层目录的规则优先级高于上层，所以这句能盖住那条。
!odoo.conf
