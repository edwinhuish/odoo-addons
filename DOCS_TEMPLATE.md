# 模块文档模板

> 本文件给出本仓库三类模块文档的统一骨架。新模块按此创建，既有模块迭代时按此对齐。
> 规范来源：根 [`AGENTS.md`](AGENTS.md) 第 6 节「文档与变更规范」。
>
> 三类文档面向不同读者：
>
> | 文档 | 面向 | 内容定位 |
> |------|------|---------|
> | `<module>/README.md` | 使用者 | 功能、字段、安装与操作步骤、验证清单 |
> | `<module>/CHANGELOG.md` | 维护者 | 逐版本「变更 / 影响 / 文档」三段式记录 |
> | `<module>/AGENTS.md` | 维护者（AI / 新开发者） | 不可破坏的核心约束、踩坑档案、文件职责、调试建议 |

---

## 模块命名约定

- 形式：`<业务域>_<功能点>`，例如 `sale_order_no`、`product_model`、`product_image`。
- 纯前端通用增强无明确业务域时，用 `web_` 技术层前缀（如 `web_image_paste`），符合 Odoo `web_*` 惯例（如 `web_editor`、`web_tour`）。
- **禁止**使用 Odoo 官方模块名（会冲突）；模型名与模块技术名不必一致（如 `product_image` 模块的模型名是 `product.image.gallery`，刻意避开 `website_sale` 的 `product.image`）。
- **不加项目统一前缀**（如 `soho_`）：会偏离 Odoo 生态命名惯例，且 Odoo 视改名模块为「不同模块」，改名需卸载重装生产库，代价过高。
- 自研模块归属识别统一用 `__manifest__.py` 的 `author: "edwinhuish"` 字段，应用列表可按 author 筛选。

---

## 一、`README.md` 模板

```markdown
# <模块显示名>

<一句话定位：基于 Odoo 19 哪个模块扩展，解决外贸 SOHO 什么场景的什么问题。>

> 模块技术名：`<module>`（如有改名历史，注明「原名 X，于 19.0.x.0.0 改名」；无则删本行）。

---

## 功能概述

- <用户可见功能点，每条一行，动词开头>
- ...

---

## 核心设计

| 设计点 | 说明 |
|--------|------|
| <设计点> | <说明，含「为什么这样做」> |
| ... | ... |

---

## 模型字段

> 纯前端模块无本节，改用「模块资源」节列静态资源文件职责。

### `<模型名>`（扩展 / 新建）

| 字段 | 类型 | 说明 |
|------|------|------|
| `field` | `Type`（required / index / store / compute...） | <中文说明> |

### 可用方法（可选）

| 方法 | 说明 |
|------|------|
| `method()` | <说明> |

---

## 视图

- **<视图名>**：<改了什么>
- ...

---

## 交互说明（可选，前端交互重的模块用）

- <交互流程 / 状态 / 快捷键说明>
- ...

---

## 依赖

- `<module>`（<为什么依赖，最小化说明>）
- <若无某依赖，注明「内建 X，无需 Y 依赖」>

---

## 安装与使用

### 全新安装

1. 将 `<module>` 目录放入 Odoo 19 的 `addons_path`
2. 更新应用列表后安装模块：`<显示名>`
3. <第一步操作>

### 从旧模块切换（如有改名 / 重构，否则删本节）

<切换步骤，或注明「无前身模块，直接全新安装」>

### 操作

- <操作要点>

---

## 验证清单

> 验收日期：YYYY-MM-DD，目标环境验证<通过 / 待验证>。

| 验证项 | 期望 | 结果 |
|--------|------|------|
| <项> | <期望> | 通过 / 待验 |

### 异常情况与处理

- <异常>：<处理>

### 后续维护

- <改动入口>：<改哪里>

---

## 许可证

LGPL-3
```

### README 约定

- 标题用模块**显示名**（中文），与 `__manifest__.py` 的 `name` 一致。
- 「核心设计」表必须含「为什么这样做」，不只是「做了什么」。
- 「验证清单」表固定三列：验证项 / 期望 / 结果；结果填「通过」或「待验」，不要空。
- 纯前端模块把「模型字段」节替换为「模块资源」节，列 `static/src/{js,xml,scss}/` 文件职责。
- 末尾固定 `## 许可证` → `LGPL-3`，与 `__manifest__.py` 一致。

---

## 二、`CHANGELOG.md` 模板

```markdown
# 变更日志

> 倒序排列，最新版本在最前。每版本固定三段式：变更 / 影响 / 文档。
> 版本号规则见根 `AGENTS.md` 第 3 节：架构/破坏性 +x，功能 +y，修复/文档 +z。

## [19.0.x.y.z] - YYYY-MM-DD（<已验证 / 待验证>）

### 变更

- <做了什么，含动机 / 根因；多相关改动用子列表归纳>
  - <子项>
- ...

### 影响

- <对用户 / 数据库 / 依赖的影响>
- <是否需要迁移脚本；如不需要，注明「不涉及数据库结构变更，无需迁移脚本」>

### 文档

- 同步更新 `__manifest__.py`、`README.md`、`AGENTS.md`、根 `TODO.md` / `README.md` / `AGENTS.md`

---

## [19.0.1.0.0] - YYYY-MM-DD（初始版本，已验证）

### 验收记录（仅首版或重大里程碑用，常规迭代删本节）

- 验收日期：YYYY-MM-DD
- 验收环境：目标 Odoo 19 部署环境
- 验收结果：<N> 项验收标准全部通过
  - [x] <验收项>

### 执行流程（仅首版用）

1. 首次安装：`odoo -d <db> -i <module> --stop-after-init`
2. <验证步骤>

### 变更

- 初始版本，实现 ...

### 影响

- <影响>

### 文档

- 同步更新 `__manifest__.py`、`README.md`、`AGENTS.md`
```

### CHANGELOG 约定

- 每个版本标题格式固定：`## [19.0.x.y.z] - YYYY-MM-DD（<已验证 / 待验证>）`，括号状态随验证进度更新。
- 三段式小标题固定：`### 变更` / `### 影响` / `### 文档`，顺序不可调。
- 「验收记录」「执行流程」仅首版或重大里程碑用；常规迭代版本只保留三段式。
- 涉及数据库结构变更必须配 `migrations/<version>/pre-migration.py`，并在「影响」注明。
- 版本之间用 `---` 分隔，保持可读性。

---

## 三、`AGENTS.md` 模板

```markdown
# AGENTS 工作指引

> 本文档用于指导 AI 助手或新开发者在维护、扩展本 Odoo 模块时的行为规范和关键上下文。
> L1 约束段每次加载必读；L2 踩坑档案按主题触发条件加载。

---

## 模块定位

- 模块名：<显示名>
- 技术目录：`<module>`
- 新建模型：`<model>`（无则注「无，纯 `_inherit` 扩展」；纯前端模块注「无，纯前端模块」）
- 继承模型：`<model>`（无则注「无」）
- 自定义组件（前端模块）：`<widget / dialog>`（无则删行）
- 主依赖：`<module>`（最小化说明，注明「不依赖 X / Y」）
- 当前版本：`19.0.x.y.z`

---

## L1：不可破坏的核心约束

每次修改代码必须保证以下行为不变。每条只列约束与违反后果，实现细节见 L2 踩坑档案。

1. **<约束标题>**
   - <约束内容>
   - 违反后果：<后果>
2. ...

---

## L2：踩坑档案（可选，按主题归档）

> 按主题归档的实现陷阱。L1 约束标注的触发条件下必读，其他时候按需查阅。
> 模块无复杂踩坑时可整节删除。

### P1：<主题>

**触发条件**：<何时必读（改某文件 / 新增某类改动时）>

**陷阱 N：<陷阱名>**
- <现象>
- 根因：<技术原因>
- 正确做法：<解法>

**最终实现**（`<文件路径>`）：

```python
<关键代码片段，可选>
```

---

## 文件职责

| 文件 | 职责 |
|------|------|
| `__manifest__.py` | <职责> |
| `models/<x>.py` | <职责> |
| `views/<x>.xml` | <职责> |
| `static/src/<x>` | <职责，前端模块> |
| `security/ir.model.access.csv` | <职责，有独立模型时> |

---

## 常见扩展场景

### <场景>

<怎么改、改哪里、不改哪里>

---

## 调试建议

- <症状>：<排查方向>

---

## 变更记录规范

每次功能修改后必须更新：
- `__manifest__.py` 的 `version`（遵循 `19.0.x.y.z`）
- `CHANGELOG.md` 的版本说明（变更 / 影响 / 文档）
- 本 `AGENTS.md` 的相关约束（若涉及行为变更）
- `README.md` 的功能说明（若涉及用户可见功能）

版本号建议：
- 破坏性变更或架构调整：升第二位，如 `19.0.2.0.0`
- 功能新增：升第三位，如 `19.0.1.1.0`
- 修复或文档：升第四位，如 `19.0.1.0.1`

---

## 开发复盘与关键经验（可选，里程碑模块用）

> <验证通过后，记录供后续类似模块复用的经验。常规迭代版本删本节。>

### 功能实现要点

- <可复用设计思路>

### 遇到的问题及解决方案

1. **<问题>**
   - 现象 / 根因 / 终解 / 经验

### 接口与字段变更

- <本里程碑引入 / 移除的模型 / 字段 / widget / 视图 / 安全规则>
```

### AGENTS 约定

- L1 约束每条**必须**含「约束内容 + 违反后果」两项；只列不可破坏的，可商量的不放 L1。
- L1 约束条目末尾标注「**改 `<文件>` 时必读 Pn**」，把 L2 触发条件显式连起来。
- L2 踩坑档案按主题（P1/P2/...）归档，每主题给「触发条件 + 陷阱 + 正确做法」，关键代码片段可选。
- 「文件职责」表必须覆盖该模块所有源码文件。
- 「开发复盘」节仅里程碑（首版、重大重构）用，常规迭代不写。
- 模块定位的「当前版本」与 `__manifest__.py` 的 `version` 必须一致，每次发版同步。

---

## 四、三类文档的同步关系

每次功能改动，按根 `AGENTS.md` 第 6 节，以下文件**缺一不可**同步更新：

```text
__manifest__.py  ──version──►  CHANGELOG.md（新增版本条目）
      │                           │
      │                           ├── 影响说明 ──► README.md（用户可见行为/字段表）
      │                           │
      └── description（用户可见行为）─┘
                                  │
                                  └── 约束变化 ──► AGENTS.md（L1 / L2）
                                                        │
                                                        └── 模块定位「当前版本」同步
```

- 改动涉及数据库结构（字段改名 / 改类型）→ 必须配 `migrations/<version>/pre-migration.py`，CHANGELOG「影响」注明。
- 改动涉及用户可见行为 → `__manifest__.py` 的 `description` 也要同步。
- 改动触及 L1 约束或踩坑 → AGENTS.md 的 L1 / L2 对应条目同步。
- 任务状态流转 → 根 `TODO.md` 同步（待办 → 进行中 → 已完成）。
