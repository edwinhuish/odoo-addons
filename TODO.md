# TODO

> 本文件是**待处理需求**的唯一入口。仅记录待办 / 进行中 / 搁置项——已完成的需求不在本文件留存，其文档与说明归档在各模块的 `README.md` / `CHANGELOG.md` / `AGENTS.md` 与根 [`README.md`](README.md) 模块一览表。
> 新需求追加到「待办池」末尾，开工移入「进行中」，验收通过后整条移出本文件（信息沉淀到模块文档）。

## 追加规则

1. **新需求一律追加到「待办池」末尾**，ID 顺序递增（`T-001` → `T-002` → …），不要插队、不要重排已有 ID。
2. 一行一项，格式：`- [ ] T-0xx ｜ 模块名 ｜ P0/P1/P2 ｜ 需求一句话描述`。
3. **开工**：整行剪切到「进行中」，并在下方补「验收标准」清单。
4. **完成**：验收通过后整条移出本文件；完成信息（日期 / 落地版本 / 验收记录 / 异常情况与后续维护）沉淀到对应模块的 `CHANGELOG.md` 与 `README.md` / `AGENTS.md`，根 `README.md` 模块一览表更新模块状态。
5. 暂时不做：移入「搁置 / 放弃」并写明原因，不删除，保留决策痕迹。
6. 一个需求对应一个模块；跨模块需求在「模块」列用 `+` 连接（如 `product_reference + web_image_paste`）。
7. 需求被拆解时，子项直接在条目下用缩进 `- [ ]` 列出，不单独占用顶层 ID。

## 状态与优先级

| 标记 | 含义 |
|------|------|
| 🔜 | 待办（在待办池中排队） |
| 🚧 | 进行中（已开工，未验收） |
| ⏸️ | 搁置 / 放弃（附原因） |
| P0 | 阻塞日常业务，优先做 |
| P1 | 重要但不阻塞 |
| P2 | 优化 / 体验类，有空再做 |

> 已完成（✅）的需求不再在本文件留存；模块当前状态见根 `README.md` 模块一览表。

---

## 进行中

- [ ] 🚧 T-009 ｜ product_packing ｜ P1 ｜ 为产品 Inventory 标签页增加产品自身尺寸（自动填充原生 Volume）与外贸纸箱字段（装箱数、长宽高、毛重、净重、自动 CBM），尺寸单位厘米 / 米可配置

  - 设计约束（动工前必读）
    - 仅扩展 `product.template`，不新建独立模型，不引入 `stock` / `sale` / `purchase` 等额外依赖
    - 字段必须嵌入原生 Inventory 标签页，与现有库存信息整合显示，不另开标签页
    - 产品尺寸放在原生 Logistics 组内；纸箱字段放在 Logistics 组之后的「纸箱与包装」组
    - CBM 由长宽高与尺寸单位自动计算，用户不可直接编辑；CBM 单位固定为立方米
    - 原生 `Volume` 仅在当前值为 0 时由产品尺寸自动填充；非 0 时不覆盖
    - 源语言 `en_US`，所有用户可见文本写英文；中文译文放 `i18n/zh_CN.po`
    - 校验信息使用 `@api.constrains` + `ValidationError`，占位符统一 `%(name)s`

  - 验收标准
    - [ ] 模块在目标环境安装成功，无报错
    - [ ] 消费品（`type = consu`）产品表单的 Logistics 组内可见 `Dimension Unit` 与 `Dimensions`
    - [ ] 输入 50×40×30 cm 后 `Volume` 自动变为 0.06；切换单位为 m 后 `Volume` 按新单位重算
    - [ ] 清空任一长宽高后 `Volume` 自动变为 0；通过导入/API 更新尺寸后 `Volume` 同样被更新
    - [ ] 消费品产品表单的 Inventory 标签页可见「纸箱与包装」组
    - [ ] 装箱数、纸箱长宽高、单位、毛重、净重可输入并保存
    - [ ] 输入 50×40×30 cm 时 CBM 自动显示为 0.06；切换为 0.5×0.4×0.3 m 时同样为 0.06
    - [ ] 负数或净重大于毛重时保存被阻止，并给出中英双语报错
    - [ ] 产品列表可选显示「纸箱」「CBM」列
    - [ ] 英文界面全部英文，切换简体中文后字段标签、报错、单位为中文

  - 备注
    - 落地版本 `19.0.1.1.1`（新模块，功能新增，第三位为 1；含 19.0.1.0.0 的纸箱字段，19.0.1.1.0 的产品尺寸，19.0.1.1.1 的尺寸变化始终同步更新 Volume）
    - 不涉及已有模型改名或数据迁移，无需迁移脚本
    - 仓库内无 Odoo 运行环境，**无法自验**，需在目标环境安装 / 升级后逐项确认；验证通过后整条移出本文件

> T-005（product_image 图片管理弹窗增强）已于 2026-09-06 完成并移除，落地版本 `19.0.2.4.2`；
> 完成信息（日期 / 落地版本 / 验收记录 / 异常与后续维护）见
> [`product_image/CHANGELOG.md`](product_image/CHANGELOG.md) →「交付记录（T-005）」，
> 坑点与解法见 [`product_image/AGENTS.md`](product_image/AGENTS.md) →「开发复盘与关键经验（T-005）」。

---



## 待办池

（空）

---

## 搁置 / 放弃

（空）
