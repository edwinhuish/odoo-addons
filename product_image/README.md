# 产品图片

Odoo 19 产品模块扩展，用于在外贸 SOHO 场景下为一个产品维护多张图片，在原有图片位置直接支持多图浏览，不新增页签，保持界面简洁。

> 模块技术名：`product_image`（原名 `product_multi_image`，于 19.0.2.0.0 改名）。模型名仍为 `product.image.gallery`（避开 `website_sale` 的 `product.image`，仅依赖 `product`）。

---

## 功能概述

- 在产品表单原有图片位置（头像区域）直接支持多图浏览，不新增页签、不增加导航层级
- **主图与图库解耦**：原生主图 `image_1920` 由原生字段独立管理（列表 / 看板 / 报价单展示它），后端图库不反向同步主图
- **展示序列**：原生主图（若有）作为浏览序列第一张，其余图库图片按 `sequence` 跟在后面；主图永远在最前（无角标，靠首位隐含）
- **列表第一位即主图**：主图为空时上传的图直接成为主图（首位）；主图已有值时上传追加为图库记录
- **删除入口只在缩略图网格，且先确认**：弹窗大图预览区（任何图片）不显示删除按钮；缩略图网格中每张图（含主图）右上角 ×——点击先弹确认框（含该图缩略图；主图提示自动提升）再删：图库图删除记录，删除主图自动提升图库首张为主图（图库空则清空主图）
- **缩略图拖动排序**（19.0.2.3.0 起，19.0.2.4.0 起含主图）：管理弹窗网格内直接拖动缩略图调整顺序（拖动时其余缩略图平滑避让，拖到哪就落到哪），松手后写回（随产品保存）。**序列第一位就是主图**：把任意一张图拖到第一位，它就成为主图；把主图拖到后面，则由上位图成为新主图（原主图自动落到它被拖到的位置，是「移动」不是复制，图片不会重复或丢失）；排序后再上传的图片仍追加到末尾
- **删除确认与批量删除**（19.0.2.3.0）：任何删除（单张右上角 ×、批量）都先弹确认框，并显示待删图片缩略图清单（主图带「主图」标签 + 自动提升提示）；header「批量删除」进入勾选模式，可多选（含主图）后一次删除（先删图库、后删主图）
- **主图名称留空**（19.0.2.3.0）：大图预览选中主图时下方名称行留空，不显示“主图”字样（固定行高占位，弹窗尺寸稳定）
- **弹窗布局：大图固定、缩略图铺满余量**：上半区大图列固定 250px（大图盒 250×250），名称单行截断 + 提示文字固定行高——切换任意图片（含有无图片）时弹窗尺寸不变；右侧缩略图网格占满除大图与间距外的全部宽高，图片较多时超高内部滚动（图片增减同样不改变弹窗高度）
- **主图放大 2 倍**（180×180），移除上一张 / 下一张按钮与序号
- **悬浮局部放大**：鼠标悬浮主图区时在左侧（空间不足转下侧 / 右侧）显示放大镜面板，只放大鼠标所在区域的局部，移动鼠标放大区域实时跟随
- **点击预览**：点击主图区弹出全屏预览弹窗，支持放大、缩小、重置、旋转（按钮 + 鼠标滚轮 + 键盘）
- **右侧竖排缩略图**：仅用于点击切换当前展示图（**编辑态缩略图不放删除按钮**，删除图库图在「图片管理」弹窗内进行）；选中项带一圈蓝色边框
- **缩略图溢出滚动**：缩略图总高超出主图高度时，顶部 / 底部出现上下滚动按钮
- **图片管理弹窗**（编辑态）：点击缩略图末端「+」占位符弹出；**上半部分**大图预览（**仅作预览、无删除按钮**）+ 平铺缩略图网格（**每张缩略图含主图右上角 × 可删**：图库图删记录，删除主图自动提升图库首张为新主图；点击缩略图只切换弹窗内大图，不影响页面主图），**下半部分**上传 dropzone（点击选文件 / 拖放图片 / Ctrl+V 粘贴三种方式；上传中每张图即时显示缩略图 + 转圈动画；Ctrl+V 粘贴上传后**不自动关闭**；**任一方式上传都不改变页面当前显示的大图**）；主图为空时上传图成为主图，否则追加图库；右上角为正方形「关闭 ×」按钮（无文字，hover 变红）
- 上传无需 `web_image_paste` 依赖，内建粘贴 / 拖放
- 每张图片继承 `image.mixin`，自动生成多尺寸（1920/1024/512/256/128）
- 产品列表展示图片数量；看板/报价单展示主图缩略（原生 `image_128` 即主图缩略）
- 删除产品时图片行级联清理，无孤儿数据
- **变体多图**（19.0.2.6.0 起）：产品变体的「独立编辑」表单（模板表单「变体」入口）右上角图片区同样启用多图 widget——**每个变体各自维护一组专属补充图**，变体之间、以及与模板共享补充图完全独立互不串扰；主图仍为原生 `image_1920`，变体无专属主图时自动回退模板主图，上传 / 替换 / 删除主图沿用 Odoo 原生逻辑
- 仅依赖 `product`，不依赖 `website_sale`，避免与 eCommerce 冲突

---

## 核心设计

| 设计点 | 说明 |
|--------|------|
| 原位多图浏览 | 自定义 `product_image_gallery` widget 替换原生 `image_1920` 字段 widget，同一位置渲染主图 + 右侧缩略图列 |
| 独立明细模型 | 补充图存于 `product.image.gallery`，继承 `image.mixin` 复用多尺寸 |
| 模型名避开 `product.image` | 刻意用 `product.image.gallery`，避免与 `website_sale` 的 `product.image` 冲突 |
| 主图独立 | 产品主图 `image_1920` 由原生字段独立管理，图库不反向同步 / 不覆盖 / 不清空；列表 / 看板展示主图 |
| 展示序列 | widget 把原生主图拼到序列第一位，图库图片按 `sequence` 跟在后面；主图永远在最前 |
| 浏览不写库 | 切换为纯前端 `currentIndex` 状态，不写库；删除主图（提升/清空）与图库增删走 One2many record 操作 |
| 悬浮放大 / 点击预览 | 均为纯前端只读行为，不写库；只读态也可用 |
| 级联清理 | 删除产品 / 变体时其图片行 `ondelete='cascade'`，无孤儿数据 |
| 变体图集独立（19.0.2.6.0） | 模板共享补充图 `image_gallery_ids`（反查 `product_tmpl_id`）；变体专属补充图走 `product.product.variant_image_gallery_ids`（反查 `product_id`）——`product.product` 经 `_inherits` 自动共享模板字段，若复用 `image_gallery_ids` 会所有变体串图；widget 按主记录模型分流图库字段（`galleryField`），主图沿用原生计算字段 `image_1920` |

---

## 模型字段

### `product.template`（扩展）

| 字段 | 类型 | 说明 |
|------|------|------|
| `image_gallery_ids` | `One2many` → `product.image.gallery` | 该产品的补充图片（不含主图），所有变体共享 |
| `image_gallery_count` | `Integer`（compute） | 图库图片数量（不含主图） |

### `product.product`（扩展，19.0.2.6.0）

| 字段 | 类型 | 说明 |
|------|------|------|
| `variant_image_gallery_ids` | `One2many` → `product.image.gallery` | 该产品变体的专属补充图片（与模板共享补充图相互独立；不含主图） |

### `product.image.gallery`（新建）

| 字段 | 类型 | 说明 |
|------|------|------|
| `image_1920` | `Image`（继承自 `image.mixin`） | 图片源数据，自动生成 1024/512/256/128 多尺寸 |
| `image_128` | `Image`（related） | 128 缩略，用于 widget 缩略图展示 |
| `name` | `Char` | 图片名称，同一归属（产品 / 产品变体）内不可重复 |
| `sequence` | `Integer` | 排序，数值小的在前；图库图片在缩略图列表中的展示顺序（主图始终在最前） |
| `active` | `Boolean` | 启用状态，可停用而不删除 |
| `note` | `Char` | 备注 |
| `product_tmpl_id` | `Many2one` → `product.template`（index, cascade） | 所属产品（模板共享补充图，所有变体共享）；与「产品变体」归属二选一 |
| `product_id` | `Many2one` → `product.product`（index, cascade，19.0.2.6.0 起） | 所属产品变体（变体专属补充图）；与「产品」归属二选一 |

---

## 视图

- **产品表单**：头像区域 `image_1920` 字段 widget 改为 `product_image_gallery`，同一位置支持多图浏览/悬浮放大/点击预览/「+」打开图片管理弹窗（删除与上传）/粘贴新增
- **产品列表**：新增「图片数」列（可选显示）
- **产品看板**：原生已展示 `image_128`（即主图缩略），无需改动
- **图库独立视图**：`产品图片` 动作，供按需检索与维护（含「产品 / 产品变体」归属列）
- **变体「独立编辑」表单**（19.0.2.6.0 起）：产品模板表单顶部「变体」入口进入变体列表并打开某变体后，右上角图片区同样为多图 widget——只作用于该变体自身的图集，模板共享补充图仍在模板表单维护

---

## 交互说明

### 展示序列
- 浏览序列第一项 = 原生主图（`image_1920` 字段值，若有），其后 = 图库图片按 `sequence` 升序。
- 主图永远在序列最前（只要有主图）；图库图片跟在后面。
- 主图区（左侧大图）显示当前选中项；默认选中第一项（主图）。
- **变体编辑表单（19.0.2.6.0 起）**：序列第一项 = 该变体的主图（原生 `image_1920`：无变体专属主图时**回退模板主图**），其后 = 该变体的专属补充图（`variant_image_gallery_ids` 按 `sequence`）；**不显示模板共享补充图**——模板共享补充图只在模板表单维护展示

### 主图区与悬浮局部放大
- 主图区按 180×180 显示（原生 90×90 的 2 倍），无上一张 / 下一张按钮、无序号。
- 鼠标悬浮主图区：在左侧（空间不足转下侧/右侧）弹出 320×320 放大镜面板，**只放大鼠标所在区域的局部**（用当前项 `image_1920` 做 background，`background-size` 放大，`background-position` 跟随鼠标相对图片内容的百分比）。移动鼠标，放大区域实时跟随。同时主图上鼠标处显示蓝色半透明方块，标示当前实际被放大的区域。
  位置优先级：左侧 → 下方 → 右侧（按可用空间自动选择）。
- 点击主图区：弹出全屏预览弹窗（预览当前选中项）。

### 预览弹窗（放大 / 缩小 / 旋转）
- **放大 / 缩小**：底部按钮，或鼠标滚轮，或键盘 `+` / `-`。
- **重置缩放**：按钮或键盘 `0`。
- **旋转**：按钮或键盘 `r`。
- **关闭**：`Esc` / `q` / 点击背景空白处 / 右上角 ×。

### 右侧缩略图列
- 缩略图 56×56，竖向排列于主图右侧；第一项为主图（无角标，靠首位隐含），其后为图库图片。
- 点击缩略图切换当前展示项（纯前端，不写库）。
- **主图删除经弹窗缩略图 ×（先确认）**：头像区缩略图列无删除 / 替换按钮；「图片管理」弹窗大图区仅作预览、无删除按钮，删除在缩略图网格——主图项亦有 ×，点击先弹确认框，确认后删除（图库首张自动提升为新主图，图库空则清空主图字段）。主图为空时上传第一张图即成为主图，主图已有值时上传追加为图库。
- 缩略图总高超出主图高度（180px）时，顶部 / 底部出现上下箭头按钮，点击滚动一屏。
- 选中缩略图带一圈蓝色边框（2px，真实 border，四周完整显示）。

### 图片管理弹窗（「+」，上半管理 + 下半上传）
- 点击缩略图末端「`+`」占位符，弹出图片管理弹窗（编辑态）。
- **上半部分**：
  - 左侧**大图预览**：跟随弹窗内选中项；**仅作预览，对任何图片都不显示删除按钮**。**大图名称行**：选中主图时留空（不显示“主图”），图库图显示其名称。
  - 右侧**平铺缩略图网格**：显示全部图片（主图 + 图库）；**每张缩略图（含主图）右上角 × 可删除（先确认）**；点击缩略图只切换弹窗内大图，**不影响页面头像区展示的主图**。
  - **拖动排序**：直接拖动图库缩略图可调整顺序——拖动时其余缩略图平滑避让、可拖到任意图库位置（拖到网格上 / 下边缘自动滚动），松手落位后写回图库 `sequence`（保存产品时一并生效）；**主图固定首位、不参与拖动**；排序后再上传的新图追加到末尾。
  - **删除图片（确认后执行）**：单张点右上角 × 弹确认框（含该图缩略图）；主图另有“图库首张自动提升 / 图库空则清空”提示。删除图库图 = 直接删该图库记录（不影响主图）；删除主图 = 图库首张自动提升为新主图（移动、不重复展示），图库为空则清空主图字段。
  - **批量删除**：modal-header 右侧「批量删除」进入勾选模式——每张缩略图左上角出现勾选圈，点击勾选（主图亦可）；header 实时显示已选数量并提供「删除 / 取消」。点「删除」先弹**批量删除确认框**：列出全部所选图片缩略图（主图带「主图」标签）+ 总数 + 主图处理提示；确认后按“先图库、后主图”顺序删除。
- **下半部分（dropzone + 上传队列）**：
  - **点击选文件**：点击拖放区域 → 唤起系统文件选择。
  - **拖放上传**：把图片文件拖到拖放区域（区域高亮），松开即上传。
  - **Ctrl+V 粘贴**：弹窗打开时焦点在弹窗上，按 `Ctrl+V` / `Cmd+V` 粘贴剪贴板图片即上传；**上传后弹窗不自动关闭**，可继续粘贴 / 管理。
  - **上传动画**：每张上传中的图片立即以本地预览显示缩略图 + 转圈动画，完成后自动移除并出现在上方网格（可连续多张）。
  - 上传规则：主图为空时上传图成为主图（首位），主图已有值时追加为图库记录。
  - **上传不改页面大图**：点击 / 拖放 / Ctrl+V 任一方式上传，都只写记录，页面头像区仍显示上传前那张图。
- 删除 / 上传后网格实时刷新，弹窗不关闭不重建，可连续操作。
- 关闭：header **最右侧正方形「×」按钮**（无文字、占满 header 高度、hover 背景变红），或 `Esc` / 点击背景空白。

---

## 国际化（i18n）

- **源语言：英文（`en_US`）**。Python / XML / JS / QWeb 模板中所有用户可见文本一律写英文，中文由译文文件提供。
- **中文译文：`i18n/zh_CN.po`**（简体中文 `zh_CN`）；模块默认展示英文，安装中文语言后界面切为中文。
- 覆盖范围：模型与字段名称 / `help`、唯一性校验报错、通知与删除确认框文案、视图标题 / 列标题 / 占位提示 / 空视图帮助、OWL 模板内联文本与 `title` / `aria-label`、JS `_t()` 文案。
- **应用列表（Apps）元数据**：模块名 / 摘要 / 描述的中文由 `i18n/zh_CN.po` 的 `model:ir.module.module,shortdesc|summary|description:base.module_product_image` 三条提供，分类另有 `model:ir.module.category,name:base.module_category_inventory_product` → 「产品」；改 `__manifest__.py` 的 `name` / `summary` / `description` 英文文案时必须同步这三条的 `msgid`，规范见根 [`AGENTS.md`](../AGENTS.md) 4.8。
- 占位符统一用命名形式 `%(name)s`（Python `_()` 与 JS `_t()` 都支持），禁止按位置 `%s` 拼接，避免翻译语序错乱。
- 模板里无法整体翻译的动态片段（如「已选 N 张」、删除按钮 `aria-label`、Ctrl+V 提示）改为在 JS 侧用 `_t()` 生成后 `t-esc` 输出，不要在模板里拼接中英文片段。
- 改动流程：改英文源文本 → 在 `i18n/zh_CN.po` 补 `msgid` / `msgstr` → `odoo -d <db> -u product_image --stop-after-init` 升级 → 强刷浏览器（前端术语有缓存）。
- 启用中文：设置 → 语言 → 安装「简体中文 (zh_CN)」，切换语言后刷新页面即可。

---

## 依赖

- `product`（产品模块，最小化依赖，不依赖 `sale` / `website_sale`）
- 粘贴新增能力内建，无需 `web_image_paste` 依赖

---

## 安装与使用

### 全新安装

1. 将 `product_image` 目录放入 Odoo 19 的 `addons_path`
2. 更新应用列表后安装模块：`产品图片`
3. 打开任意产品表单，头像区域即多图控件

### 从旧模块 `product_multi_image` 切换

旧模块 `product_multi_image`（19.0.1.0.0）在目标环境**尚未验证安装**，无生产数据需迁移。
切换方式：

1. 在目标环境**卸载** `product_multi_image`（设置 → 应用 → 卸载，或命令行卸载）。卸载会级联删除 `product.image.gallery` 表数据（仅测试数据）。
2. 将 `product_image` 目录放入 `addons_path`，更新应用列表。
3. 安装模块：`产品图片`。

> 不需要写迁移脚本：模块技术名变更在 Odoo 中视为「不同模块」，新模块全新安装即可；
> 旧模块未在目标环境安装过，无字段 / 数据需迁移。

### 操作
- 点击缩略图末端「`+`」占位符弹出「图片管理」弹窗：上半部分大图预览（**无删除按钮，仅预览；选中主图时名称行留空**）+ 平铺缩略图网格（**每张缩略图含主图右上角 ×，点击先确认后删除**；删图库图删记录，删主图自动提升图库首张；**直接拖动图库缩略图可排序**，主图固定首位），下半部分 dropzone 上传（点击选文件 / 拖放 / Ctrl+V 粘贴，上传中即时显示缩略图与动画，粘贴后不自动关闭）
- header 右侧「批量删除」：进入勾选模式逐张勾选（含主图），header 显示已选数量，确认框列出全部所选缩略图后一次删除（先删图库、后删主图）
- 弹窗内点击缩略图只切换弹窗大图，不影响页面头像区展示；任一方式上传都不改变页面大图；右上角正方形「关闭 ×」按钮 / `Esc` / 点背景关闭
- 主图为空时上传第一张图直接成为主图；已有主图时上传追加为图库（主图 / 图库图均可在弹窗缩略图右上角 × 删除——先确认，删除主图自动提升图库首张）
- 多图时点击右侧缩略图切换当前展示项（选中项蓝色边框），悬浮主图区查看放大，点击主图区进入全屏预览
- 保存产品后：主图由原生字段管理，列表/看板展示主图；图库只管补充图
- **变体（19.0.2.6.0 起）**：打开多属性产品模板表单 → 顶部「变体」进入变体列表 → 打开某变体，右上角图片区即可独立维护该变体的图片集——各变体图集互不影响，也不与模板共享补充图串扰；模板表单图片区继续维护模板共享补充图

---

## 验证清单

> 目标环境已验证通过（2026-09-04，落地版本 `19.0.2.2.7`）。
> `19.0.2.2.10 / 19.0.2.2.11`（2026-09-05）为前端样式修复：缩略图滚动不再越出列边界，且行与 180px 区顶底贴边对齐。
> `19.0.2.2.12`（2026-09-05）移除头像区缩略图删除按钮，「+」改开「图片管理」弹窗（上半大图+平铺缩略图可删除，下半上传 dropzone）。
> `19.0.2.2.13`（2026-09-05）修复该弹窗打开即报错（模板 `items.length` 需组件暴露 `items` getter，见 `product_image_manage.js`），其余界面不变。
> `19.0.2.2.14`（2026-09-05）管理弹窗交互调整：主图不显示删除按钮（仅图库图可删）、点击缩略图只切换弹窗内大图（不影响页面主图）、粘贴上传后不自动关闭、上传即时显示缩略图 + 转圈动画、关闭按钮为右上角按钮形态。
> `19.0.2.2.15`（2026-09-05）管理弹窗再调整：大图预览区移除删除按钮（删除只在缩略图网格，并**恢复主图缩略图右上角 ×**——删除主图自动提升图库首张）、关闭按钮改为正方形无文字并位于 header 最右侧（hover 背景变红）、任一方式上传都不再改变页面当前显示的大图。
> `19.0.2.4.3`（2026-09-06）修复缩略图列末端「+」占位符被压成约 18px 的扁条：列向 flex 容器溢出时该项被 `flex-shrink` 压缩（其 min-content 高度仅图标行高）；现禁止收缩并以 `min-width/min-height: 56px` 兜底，**始终保持 56×56 正方形**。
> `19.0.2.4.2`（2026-09-06）弹窗右上角关闭按钮贴齐最右侧：移除 header 右侧操作区的 `pe-1`（4px 右内边距），关闭按钮与弹窗右边缘完全对齐。
> `19.0.2.4.1`（2026-09-06）删除确认框底部按钮顺序调整：「删除」在左、**「取消」在最右侧**（最易点击），样式与交互逻辑不变。
> `19.0.2.4.0`（2026-09-06）① **主图也可拖动排序**：序列首位即主图——把图拖到首位即设为主图，把主图拖到后面则由上位图成为新主图（移动语义：被提升的图库记录删除、原主图新建为图库记录，图片不重复不丢失）；写回改为「统一数组 diff + 最小写回」（只写位置真的变了的图库记录；长度不一致即放弃，防并发增删错位）；② 修复「批量删除」进入勾选模式后看不到「已选 N 张 / 删除 / 取消」按钮（模板误用 `<template t-if>`，HTML 惰性容器内容不渲染）。
> `19.0.2.3.2`（2026-09-06）修复拖动排序布局：① **拖动不再产生巨大间隙与横向滚动条**——根因是缩略图上的 Bootstrap `.position-relative`（`position: relative !important`）压过了排序态的 `position: absolute`，tile 未脱离流内、`transform` 叠加在流内位置之上；现常态定位由模块 SCSS 提供，并给网格加 `overflow-x: hidden`；② 排序首帧不再“全部缩略图从左上角飞入”（`is-sorting-init` 禁用过渡 + `onPatched` 切阶段）；③ 拖拽块按**抓取点偏移**跟随鼠标（按住哪一点就跟着哪一点），抓取与移动全程不跳位。
> `19.0.2.3.1`（2026-09-06）修复：① 拖拽块逐帧 1:1 跟手（拖动中去掉自身过渡，松手瞬间加 `o_gm-snap` 保留落位动画）；② 拖动全程不闪现滚动条、无整网格重排跳变（排序占位高度改为与原 flex 布局精确等高）；③ 块可拖到末行 / 内容底，且与鼠标精确对齐（坐标计入网格边框）；④ 修复管理弹窗模板 `&nbsp;` 实体致前端资源加载崩溃（改为 `&#160;`）。
> `19.0.2.3.0`（2026-09-05）管理弹窗增强：大图选中主图时名称行留空（不显示“主图”）、缩略图**拖动排序**（避让动画、顺序写回图库 `sequence`）、删除**先确认**（单张确认含缩略图）、header「批量删除」勾选模式 + **批量删除确认**（缩略图清单）；并修复图库删除 `g<id>` key 匹配失效。
> 以上（含 `19.0.2.2.10~2.2.15`）均为前端改动，已于 **2026-09-06 在目标环境验证通过**（落地 `19.0.2.4.2`，见 T-005 交付记录）；坑点与解法见 `AGENTS.md` →「开发复盘与关键经验（T-005）」。
> `19.0.2.5.0`（2026-09-07）完成国际化（T-006）：源语言改为英文 + `i18n/zh_CN.po` 中英双语，目标环境已验证通过。
> `19.0.2.6.0`（2026-09-08）完成产品变体多图（T-010）：`product.image.gallery` 增加 `product_id` 变体维度（产品 / 变体归属二选一）、`product.product` 新增 `variant_image_gallery_ids`，变体「独立编辑」表单图片区启用多图 widget——**目标环境验收通过（2026-09-08，随 19.0.2.6.1 复验，验收记录见 CHANGELOG →「交付记录（T-010）」）**。
> `19.0.2.6.1`（2026-09-08）修复 19.0.2.6.0 回归：图库 One2many 需经 widget `fieldDependencies` 加载子数据（invisible x2many 不在主加载 spec 取 children），恢复该机制并按 `image_1920` options `gallery_field` 分流产品 / 变体图库字段——**目标环境复验通过（2026-09-08）**：产品 / 变体表单图库图片全部正常显示、不再出现占位符（验收记录见 CHANGELOG →「交付记录（T-010）」）。
> `19.0.2.6.2`（2026-09-08）修复变体上传补充图报「双归属」Validation Error：变体编辑 action context 携带 `default_product_tmpl_id`（官方 product 变体列表 action），图库子行 `(0, 0)` 创建时子模型 default_get 把它填进 `product_tmpl_id`，与 One2many inverse 回填的 `product_id` 冲突——`product.product` 的 create / write 现对 `variant_image_gallery_ids` 的 `(0,0)` 创建命令子行强制置空 `product_tmpl_id`——**待目标环境复验**。
> `19.0.2.6.3`（2026-09-09，T-013）补应用列表（Apps）中文元数据（模块名 / 摘要 / 描述 + 分类「产品」），**目标环境已验收通过**；记录见 `CHANGELOG.md` →「验收记录（T-013）」。

| 验证项 | 期望 |
|--------|------|
| 主图作为首张 | 有主图时浏览序列第一项即主图（无角标），图库图片按序跟在后面 |
| 首张即主图 | 空产品上传第一张图直接成为主图；已有主图时上传追加为图库（不影响主图） |
| 主图删除入口 | 弹窗大图区无删除按钮；缩略图网格中主图项亦有 ×——删除主图时图库首张自动提升为新主图（图库空则清空） |
| 主图 2 倍显示 | 头像区域主图 180×180，无上一张 / 下一张按钮、无序号 |
| 悬浮局部放大 | 鼠标悬浮主图区弹出 540×540 放大镜（内含 1080 图），左侧空间不足转下方、下方仍不足按比例缩小；只放大鼠标所在区域，移动鼠标放大区域跟随；鼠标位置与放大区域精确对应 |
| 蓝色指示方块 | 悬浮放大时主图上鼠标处显示蓝色半透明方块，边长按 (窗口/图片) 比例动态调整，标示实际被放大的区域 |
| 点击预览 | 点击主图区弹出全屏预览：放大 / 缩小 / 重置 / 旋转可用（按钮 + 滚轮 + 键盘），任意大小可拖拽（grab/grabbing），切换图片保留各图状态，无滚动条 |
| 缩略图切换 | 右侧竖排缩略图点击切换当前展示项（编辑态缩略图上无删除按钮） |
| 新增图片占位符 | 编辑态缩略图列末端「+」为 **56×56 正方形**（图片很多、列需滚动时也不被压扁），点击打开图片管理弹窗；只读态不显示 |
| 选中边框 | 选中缩略图带一圈蓝色 border（未选中无任何边框，不抖动） |
| 管理弹窗打开 | 缩略图末端「+」点击弹出图片管理弹窗（上半大图 + 平铺缩略图网格，下半 dropzone） |
| 弹窗缩略图网格 | 网格平铺展示全部图片（主图+图库）；每张缩略图（含主图）右上角红色 × 完整显示、不被裁切 |
| 弹窗删除图库图 | 网格删除图库记录（不影响主图），列表实时刷新 |
| 弹窗切换大图 | 点击网格缩略图，左侧大图跟随切换，**页面头像区展示不被影响** |
| 大图无删除按钮 | 弹窗左侧大图区对任何图片均不显示删除按钮（仅作预览） |
| 主图名称行留空 | 大图预览选中主图时名称行留空（不显示“主图”），弹窗尺寸与图库图一致；切回图库图显示名称 |
| 拖动排序 | 缩略图网格内拖动缩略图（**含主图**）：其余缩略图平滑避让、可拖到任意位置（边缘自动滚动），松手落位；把图拖到首位即设为主图，把主图拖到后面则由上位图成为新主图（移动语义，图片不重复不丢失） |
| 拖动布局 | 拖动全程缩略图保持原有 8px 间距、不产生额外间隙，网格不出现横向滚动条；排序首帧无“全部缩略图从左上角飞入” |
| 拖拽跟手 | 拖拽块保持按住的那一点贴合鼠标：抓取瞬间不跳位、移动全程不滞后，松手平滑落位 |
| 排序写库 | 拖动排序后保存产品，重开弹窗顺序与新主图均保持；只写位置真的变了的图库记录（未变动不产生写操作）；排序后再上传的新图追加末尾 |
| 单删确认 | 点缩略图右上角 × 先弹确认框（含该图缩略图；主图有自动提升提示），取消不删、确认才删 |
| 批量删除 | header「批量删除」进入勾选模式（勾选圈 + 已选计数），header 同步出现「删除 / 取消」；确认框列出全部所选缩略图（主图带「主图」标签）后一次删除；含主图时先删图库后删主图 |
| 确认框按钮 | 删除确认框底部从左到右为「删除」（红色带图标）→「取消」（灰色，**最右侧**）；`Esc` / 点遮罩 / 右上角 × 均可取消 |
| 图库删除修复 | 图库记录右上角 × 确认删除后记录真实删除（`g<id>` key 解析，非仅弹窗内消失） |
| 弹窗点击上传 | 点击 dropzone 区域可唤起系统文件选择 |
| 弹窗拖放上传 | 拖放图片到 dropzone 区域（高亮）即上传 |
| 上传缩略图动画 | 点击 / 拖放 / Ctrl+V 上传时，每张上传中的图即时出现缩略图 + 转圈动画，完成后自动移除并入网格 |
| 弹窗 Ctrl+V | 弹窗打开时按 Ctrl+V 粘贴剪贴板图片即上传（弹窗获焦，无需再点），**上传后弹窗不自动关闭** |
| 上传不改页面大图 | 点击 / 拖放 / Ctrl+V 任一方式上传，关闭弹窗后页面头像区仍显示上传前那张图 |
| 弹窗关闭按钮 | 右上角为正方形 × 按钮（无文字、占满 header 高、hover 背景变红），**右缘与弹窗边缘完全贴齐（无右内边距）**，点击关闭；`Esc` / 点背景亦可关闭 |
| 头像区删除移除 | 头像区缩略图列悬浮不再出现删除按钮；删除（图库图 / 主图）均经「图片管理」弹窗缩略图 × |
| 缩略图滚动 | 缩略图超出主图高度时出现上下滚动按钮，点击可滚动；滚动过程中缩略图不“突出”到滚动按钮 / 缩略图列边界之外；少量图（无滚动按钮）时首行与主图区顶边平齐 |
| 只读态 | 无「+」占位符；悬浮放大与点击预览仍可用 |
| 主图独立 | 图库增删改不影响产品主图；列表/看板展示原生主图 |
| 产品列表图片数 | 「图片数」列显示正确数量 |
| 删除产品 | 图片行随之级联清理 |
| 重复图片名称 | 同一产品 / 同一产品变体内被阻止并中文提示（跨产品 / 跨变体同名可共存） |
| 图库独立动作 | 可打开列表/表单检索维护（含「产品 / 产品变体」归属列） |
| 变体图集独立（19.0.2.6.0） | 多属性模板：在变体 A 表单上传的补充图只属于 A，不出现在变体 B、也不出现在模板共享图库；模板共享补充图不出现在任何变体表单 |
| 变体主图回退 | 变体无专属主图时图片区显示模板主图（回退），上传该变体专属主图后优先显示变体图 |
| 变体主图落点 | 多变体上传主图写该变体专属主图；模板只有一个活动变体时写模板主图（原生 `_set_template_field` 行为，全局可见） |
| 变体图集操作 | 变体表单图片区上传 / 删除（删主图自动提升图库首张）/ 拖动排序 / 批量删除 / 悬浮放大 / 全屏预览均可用，且只作用于该变体 |
| 模板表单不回归 | 模板表单图片区行为与 19.0.2.5.0 一致（模板共享补充图 / 主图操作不变） |
| 删除变体 | 删除变体时其专属图片行级联清理 |
| 模块切换 | 卸载 `product_multi_image` → 安装 `product_image` 后功能正常 |
| 应用列表中文名（19.0.2.6.3，已验收） | 中文环境「应用」搜 `product_image`，卡片标题显示「产品图片」，摘要与详情描述为中文，左侧分类显示「库存 / 产品」；英文环境仍为英文 |

### 执行流程

1. 首次安装：`odoo -d <db> -i product_image --stop-after-init`
   自动建表、加载权限、注册 `product_image_gallery` widget 前端资源
2. 产品表单头像区域验证主图作为首张、2 倍显示、悬浮放大、点击预览（放大/缩小/旋转）
3. 多图时验证右侧缩略图切换、滚动；点「+」打开图片管理弹窗（大图区无删除按钮、点击缩略图只切弹窗大图、点击/拖放/Ctrl+V 上传且即时显示缩略图 + 动画）
4. 管理弹窗增强验证：选中主图大图名称行留空（切回图库图显示名称）；拖动缩略图排序（**含主图**：拖到首位即设为主图、拖主图到后面则由上位图成为新主图；其余平滑避让、可拖到末尾、无额外间隙与横向滚动条、拖拽块跟手）并保存产品后重开确认顺序与新主图均保持、之后上传的新图追加末尾；点缩略图右上角 × 先弹确认（含缩略图与主图提示）；header「批量删除」勾选多张（header 出现「已选 N 张 / 删除 / 取消」）→ 批量确认框含全部缩略图清单 → 确认后删除且页面 / 网格同步
5. 在图片管理弹窗内粘贴图片验证新增图库记录且**弹窗不自动关闭**，可连续粘贴多张；关闭弹窗后页面大图仍为上传前那张（**上传不改变页面大图**）
6. 验证主图独立：上传/删除图库图片，产品主图（列表/看板）不变
7. 删除产品验证级联清理
8. 变体多图（19.0.2.6.0）：打开含多属性的模板表单 → 顶部「变体」按钮进入变体列表 → 打开某变体——右上角图片区为多图 widget：上传多张补充图（只属该变体），另一变体与模板共享补充图不受影响；无变体专属主图时图片区显示模板主图（回退）；删除（删主图自动提升图库首张）/ 拖动排序 / 批量删除 / 悬浮放大 / 全屏预览均验证；返回模板表单确认图片区行为与旧版一致（不回归）
9. 回归修复验证（19.0.2.6.1）：升级后先打开一个已有若干张补充图的产品模板表单——主图与图库全部图片均正常显示（不再出现占位符）；再打开某变体编辑表单确认其图库同样正常；随后复验上传 / 删除 / 排序等写操作不回归
10. 双归属回归验证（19.0.2.6.2）：模板表单 → 顶部「变体」进入变体列表 → 打开某变体 → 上传若干张补充图并保存：不再弹 “cannot belong to both a product and a product variant…” 双归属报错；返回模板表单确认共享补充图上传 / 保存不受影响

### 异常情况与处理

- 主图与图库均无值：头像区域显示占位图与「暂无图片」提示
- 历史产品有主图无图库：主图作为序列首张展示，无图库缩略图；上传图库图后跟在主图后面（不影响主图）
- 同一产品重复图片名称：`@api.constrains` 阻止并中文提示
- 粘贴无反应：粘贴入口在「图片管理」弹窗内（打开后自动获焦，按 `Ctrl+V` 粘贴），头像区域本身不再直接响应粘贴
- widget 不生效：前端资源缓存，`-u product_image` 升级后强刷浏览器
- 变体表单看不到多图 widget：需从模板表单「变体」入口打开（该表单才带 easy edit 图片区），且升级后强刷浏览器
- 变体图集与模板共享图串图：变体补充图必须挂在 `product_id`（不写 `product_tmpl_id`）；widget 数据源见 `galleryField`（AGENTS.md L1 约束 2 / 9 / 11）
- 切换不写库：浏览切换是纯前端状态，不触发保存；图库图删除、主图删除（自动提升图库首张）、上传、拖动排序才写库（均需产品表单保存后落库）
- 缩略图无法拖动排序：需 ≥2 张图片（主图也可拖）；处于批量删除勾选模式、或删除确认框打开时不支持拖动（先退出）
- 拖动排序后顺序未保存：拖动只更新图库记录 `sequence`（以及首位变化时的主图字段）为待保存变更，需在产品表单点「保存」后写库
- 拖动出现巨大间隙 / 横向滚动条：缩略图**不能**加 Bootstrap 的 `.position-relative`（`!important` 会压过排序态 `position:absolute`）；定位由模块 SCSS 提供
- 勾选模式看不到操作按钮：QWeb 条件分支必须用 `<t t-if>`，`<template t-if>` 是 HTML 惰性容器、内容不渲染
- 整个后台白屏 / `Entity 'nbsp' not defined`：QWeb 模板按 XML 解析，禁用 HTML 命名实体（用 `&#160;`），改完需 `-u` 升级并强刷

### 后续维护

- 图库排序规则：改 `product.image.gallery` 的 `_order` 与 widget `galleryRecords` getter 排序逻辑
- 变体图集数据源（19.0.2.6.0）：widget 的 `galleryField` getter 按主记录 `resModel` 分流——`product.template` → `image_gallery_ids`，`product.product` → `variant_image_gallery_ids`；新增入口模型时只改此处
- 排序写回逻辑：统一在 widget 的 `onManageReorder` / `_writeOrderWithNewMain` / `_writeGalleryOrder`（统一数组 diff + 最小写回），不要在各处散写 sequence
- 坑点与解法索引：见 `AGENTS.md` →「开发复盘与关键经验（T-005）→ 遇到的问题及解决方案」（含 Bootstrap `!important`、`<template>` 惰性容器、`overflow-y:auto` 隐式横向滚动、binary size 图片需 ORM 读取、未保存记录虚拟 id 等）
- widget 行为调整：改 `static/src/js/product_image_gallery.js` 与对应 QWeb 模板
- 预览弹窗调整：改 `static/src/js/product_image_preview.js` 与 `static/src/xml/product_image_preview.xml`
- 与 `website_sale` 共存：本模块用 `product.image.gallery` 模型名，与 `product.image` 互不干扰

---

## 许可证

LGPL-3
