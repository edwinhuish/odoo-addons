# 产品图片

Odoo 19 产品模块扩展，用于在外贸 SOHO 场景下为一个产品维护多张图片，在原有图片位置直接支持多图浏览，不新增页签，保持界面简洁。

> 模块技术名：`product_image`（原名 `product_multi_image`，于 19.0.2.0.0 改名）。模型名仍为 `product.image.gallery`（避开 `website_sale` 的 `product.image`，仅依赖 `product`）。

---

## 功能概述

- 在产品表单原有图片位置（头像区域）直接支持多图浏览，不新增页签、不增加导航层级
- **主图放大 2 倍**（180×180），移除上一张 / 下一张按钮与序号
- **悬浮放大**：鼠标悬浮主图时在左侧（空间不足转下侧 / 右侧）显示放大的图片
- **点击预览**：点击主图弹出全屏预览弹窗，支持放大、缩小、重置、旋转与**复制图片到剪贴板**
- **右侧竖排缩略图**：每张带删除按钮（悬浮显示），点击切换主图
- **缩略图溢出滚动**：缩略图总高超出主图高度时，顶部 / 底部出现上下滚动按钮
- **缩略图末端上传占位符**（编辑态）：点击即新增图片，一次可选多张
- 头像区域支持 `Ctrl+V` 粘贴剪贴板图片即新增图库记录（无需 `image_uploader` 依赖）
- 每张图片继承 `image.mixin`，自动生成多尺寸（1920/1024/512/256/128）
- 首图（排序最前）自动同步到产品主图 `image_1920`，保留与原生主图的关系
- 产品列表展示图片数量；看板/报价单展示主图缩略（原生 `image_128` 即主图缩略）
- 删除产品时图片行级联清理，无孤儿数据
- 仅依赖 `product`，不依赖 `website_sale`，避免与 eCommerce 冲突

---

## 核心设计

| 设计点 | 说明 |
|--------|------|
| 原位多图浏览 | 自定义 `product_image_gallery` widget 替换原生 `image_1920` 字段 widget，同一位置渲染主图 + 右侧缩略图列 |
| 独立明细模型 | 图片存于 `product.image.gallery`，继承 `image.mixin` 复用多尺寸 |
| 模型名避开 `product.image` | 刻意用 `product.image.gallery`，避免与 `website_sale` 的 `product.image` 冲突 |
| 首图即主图 | `sequence` 最小且 `id` 最小者即主图，`_sync_main_image_from_template` 自动同步到 `product.template.image_1920` |
| `is_main` 自动判定 | 由排序与 `active` 自动计算，勿手工编辑 |
| 浏览不写库 | 切换为纯前端 `currentIndex` 状态，不写产品主图；上传/删除走 One2many record 操作 |
| 悬浮放大 / 点击预览 | 均为纯前端只读行为，不写库；复制图片到剪贴板是只读操作，只读态也可用 |
| 级联清理 | 删除产品时图片行 `ondelete='cascade'`，无孤儿数据 |

---

## 模型字段

### `product.template`（扩展）

| 字段 | 类型 | 说明 |
|------|------|------|
| `image_gallery_ids` | `One2many` → `product.image.gallery` | 该产品的所有图片 |
| `image_gallery_count` | `Integer`（compute） | 图片数量 |

### `product.image.gallery`（新建）

| 字段 | 类型 | 说明 |
|------|------|------|
| `image_1920` | `Image`（继承自 `image.mixin`） | 图片源数据，自动生成 1024/512/256/128 多尺寸 |
| `image_128` | `Image`（related） | 128 缩略，用于列表/看板/widget 缩略图展示 |
| `name` | `Char` | 图片名称，同一产品内不可重复 |
| `sequence` | `Integer` | 排序，数值小的在前；首图即主图 |
| `is_main` | `Boolean`（compute, store） | 是否为主图，自动判定 |
| `active` | `Boolean` | 启用状态，可停用而不删除 |
| `note` | `Char` | 备注 |
| `product_tmpl_id` | `Many2one` → `product.template`（required, index, cascade） | 所属产品 |

---

## 视图

- **产品表单**：头像区域 `image_1920` 字段 widget 改为 `product_image_gallery`，同一位置支持多图浏览/悬浮放大/点击预览/上传/删除/粘贴新增
- **产品列表**：新增「图片数」列（可选显示）
- **产品看板**：原生已展示 `image_128`（即主图缩略），无需改动
- **图库独立视图**：`产品图片` 动作，供按需检索与维护

---

## 交互说明

### 主图与悬浮放大
- 主图按 180×180 显示（原生 90×90 的 2 倍），无上一张 / 下一张按钮、无序号。
- 鼠标悬浮主图：弹出放大图（用 `image_1920` 源数据，最大 320×320）。
  位置优先级：左侧 → 下方 → 右侧（按可用空间自动选择）。
- 点击主图：弹出全屏预览弹窗。

### 预览弹窗（放大 / 缩小 / 复制）
- **放大 / 缩小**：底部按钮，或鼠标滚轮，或键盘 `+` / `-`。
- **重置缩放**：按钮或键盘 `0`。
- **旋转**：按钮或键盘 `r`。
- **复制图片到剪贴板**：顶部 / 底部「复制」按钮。
  把当前预览图片（data URL 或同源 `web/image` URL）写入系统剪贴板，
  可在聊天、文档、其他图片字段位置 `Ctrl+V` 粘贴出图片。
  浏览器不支持时给出中文报错，建议改用「下载」。
- **关闭**：`Esc` / `q` / 点击背景空白处 / 右上角 ×。

### 右侧缩略图列
- 缩略图 56×56，竖向排列于主图右侧。
- 点击缩略图切换主图（纯前端，不写库）。
- 悬浮缩略图显示右上角 × 删除按钮，点击删除该图片（可删任意一张，不限于当前主图）。
- 缩略图总高超出主图高度（180px）时，顶部 / 底部出现上下箭头按钮，点击滚动一屏。
- 末端有虚线上传占位符（`+`），点击选择图片文件新增（一次可选多张）。

### 粘贴新增
- 头像区域获得焦点后（按 `Tab` 切到图片区域），按 `Ctrl+V` / `Cmd+V` 可直接粘贴剪贴板图片，每张自动新增一条图库记录并设为当前图。

---

## 依赖

- `product`（产品模块，最小化依赖，不依赖 `sale` / `website_sale`）
- 粘贴新增能力内建，无需 `image_uploader` 依赖

---

## 安装与使用

### 全新安装

1. 将 `product_image` 目录放入 Odoo 19 的 `addons_path`
2. 更新应用列表后安装模块：`产品图片`
3. 打开任意产品表单，头像区域即多图控件

### 从旧模块 `product_multi_image` 切换

旧模块 `product_multi_image`（19.0.1.0.0）在目标环境**尚未验证安装**，无生产数据需迁移。
切换方式：

1. 在目标环境**卸载** `product_multi_image`（设置 → 应用 → 卸载，或命令行卸载）。卸载会级联删除 `product.image.gallery` 表数据（仅测试数据）。
2. 将 `product_image` 目录放入 `addons_path`，更新应用列表。
3. 安装模块：`产品图片`。

> 不需要写迁移脚本：模块技术名变更在 Odoo 中视为「不同模块」，新模块全新安装即可；
> 旧模块未在目标环境安装过，无字段 / 数据需迁移。

### 操作
- 点击缩略图末端虚线上传框选择图片文件上传，自动新增图库记录
- 多图时点击右侧缩略图切换，悬浮主图查看放大，点击主图进入全屏预览
- 删除按钮（悬浮缩略图显示）移除指定图片
- 头像区域获得焦点后按 `Ctrl+V` 粘贴剪贴板图片新增
- 保存产品后首图自动同步为产品主图，列表/看板展示主图

---

## 验证清单

> 待目标环境验证。

| 验证项 | 期望 |
|--------|------|
| 主图 2 倍显示 | 头像区域主图 180×180，无上一张 / 下一张按钮、无序号 |
| 悬浮放大 | 鼠标悬浮主图弹出放大图（左侧优先，空间不足转下侧 / 右侧） |
| 点击预览 | 点击主图弹出全屏预览：放大 / 缩小 / 重置 / 旋转可用（按钮 + 滚轮 + 键盘） |
| 复制图片 | 预览弹窗「复制」按钮把图片写入系统剪贴板，可在其他位置 Ctrl+V 粘贴出图片 |
| 缩略图切换 | 右侧竖排缩略图点击切换主图 |
| 缩略图删除 | 缩略图悬浮显示删除按钮，点击删除对应图片并维持位置 |
| 缩略图滚动 | 缩略图超出主图高度时出现上下滚动按钮，点击可滚动 |
| 上传占位符 | 缩略图末端有上传占位符，点击可新增图片（一次多图） |
| 粘贴新增 | 头像区域 Ctrl+V 粘贴剪贴板图片新增图库记录 |
| 只读态 | 无上传占位符 / 删除按钮 / 粘贴；悬浮放大与点击预览（含复制）仍可用 |
| 首图同步 | 保存产品后首图同步到主图，列表/看板展示主图 |
| 产品列表图片数 | 「图片数」列显示正确数量 |
| 删除产品 | 图片行随之级联清理 |
| 重复图片名称 | 同一产品内被阻止并中文提示 |
| 图库独立动作 | 可打开列表/表单检索维护 |
| 模块切换 | 卸载 `product_multi_image` → 安装 `product_image` 后功能正常 |

### 执行流程

1. 首次安装：`odoo -d <db> -i product_image --stop-after-init`
   自动建表、加载权限、注册 `product_image_gallery` widget 前端资源
2. 产品表单头像区域验证主图 2 倍显示、悬浮放大、点击预览（含复制）
3. 多图时验证右侧缩略图切换、删除、滚动、上传占位符
4. 头像区域粘贴图片验证新增
5. 保存产品验证首图同步
6. 删除产品验证级联清理

### 异常情况与处理

- 图库为空：头像区域显示占位图与「暂无图片」提示，产品主图 `image_1920` 被清空
- 历史产品无图库：`image_gallery_ids` 为空，头像区域显示占位图，不影响原生主图字段使用
- 同一产品重复图片名称：`@api.constrains` 阻止并中文提示
- 粘贴无反应：检查头像区域是否获得焦点（根 div 有 `tabindex="0"`），按 `Tab` 切到图片区域后再粘贴
- 复制失败：浏览器不支持复制图片到剪贴板（如非 HTTPS / 老版本），改用「下载」
- widget 不生效：前端资源缓存，`-u product_image` 升级后强刷浏览器
- 切换不写库：浏览切换是纯前端状态，不触发保存；上传/删除才写库

### 后续维护

- 改变首图判定规则：只改 `product_image.py` 的 `_compute_is_main` 与 `_get_main_image`
- widget 行为调整：改 `static/src/js/product_image_gallery.js` 与对应 QWeb 模板
- 预览弹窗调整：改 `static/src/js/product_image_preview.js` 与 `static/src/xml/product_image_preview.xml`
- 与 `website_sale` 共存：本模块用 `product.image.gallery` 模型名，与 `product.image` 互不干扰

---

## 许可证

LGPL-3
